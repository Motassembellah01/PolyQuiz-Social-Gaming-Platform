version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  command: node -r module-alias/register out/server/app/index.js
  network:
    port: 3000
    env: plain
  env:
    - name: PORT
      value: "3000"
